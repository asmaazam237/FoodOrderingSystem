# FoodOrderingSystem
Desktop application built using Java Swing and MySQL database.

Allows users to browse food items and add to cart.

Users can place orders through a simple interface.

Admin can manage menu items with login access.

Uses core OOP concepts (encapsulation, inheritance, abstraction, polymorphism).
